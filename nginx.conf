upstream backend {
		server app:1323;
}

server {
		listen 80;
		server_name outagealert.dev;
		add_header Access-Control-Allow-Origin *;
		root /var/;

		location = /sitemap.xml {
				alias /var/sitemap.xml;
		}

		location = /robots.txt {
				alias /var/robots.txt;
		}

		location / {
				proxy_pass http://backend;
		}

}
